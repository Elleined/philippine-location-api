# ===============================
# DataSource & HikariCP Settings
# ===============================
spring.datasource.url=jdbc:mysql://${MYSQL_HOST}:${MYSQL_HOST_PORT}/${MYSQL_DATABASE}
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}